现在每 1000 step 保存的 2x2 图含义如下。

左上：`Reliability rho`
- x 轴：`rho`，可靠性权重，范围 0 到 1。
- y 轴：density，归一化后的密度，不是样本数。
- 蓝色：正例权重，也就是同一样本两种增强视图的可靠性。
- 红色：负例权重，也就是当前实现里认为“更像真负例”的权重。
- 含义：看结构可靠性方法有没有把正例和负例分出不同权重。如果蓝红高度重叠，说明权重区分度弱。

右上：`Discriminator logits`
- x 轴：logit，判别器原始输出分数。
- y 轴：density，分数分布密度。
- 蓝色：正例 logits。
- 红色：负例 logits。
- 含义：看模型是否真的把正例打高、负例打低。理想情况是蓝色整体偏右，红色整体偏左。

左下：`Prediction probability`
- x 轴：`sigmoid(logit)`，把 logit 转成 0 到 1 的概率。
- y 轴：density，概率分布密度。
- 蓝色：正例预测为正的概率。
- 红色：负例预测为正的概率。
- 含义：更直观看 BCE 是否已经饱和。理想情况是蓝色接近 1，红色接近 0。如果很早就这样，说明 loss 后面基本没梯度了。

右下：`Loss contribution`
- x 轴：`weighted BCE contribution`，每个样本对最终 loss 的贡献，即 `BCE(logit, label) * rho`。
- y 轴：density，贡献值分布密度。
- 蓝色：正例对 loss 的贡献。
- 红色：负例对 loss 的贡献。
- 含义：看训练主要被哪些样本推动。如果两边都挤在 0 附近，说明训练目标已经饱和；如果红色贡献大，说明负例还在主导优化；如果蓝色贡献大，说明正例没学好。